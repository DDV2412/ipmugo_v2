const { resolve } = require("path");

module.exports = {
  config: resolve("src/config", "config.js"),
  "migrations-path": resolve("src/database", "migrations"),
  "seeders-path": resolve("src/database", "seeds"),
  "models-path": resolve("src/models", "models"),
};
